name: ubuntu

on:
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: build
        run: git clone https://github.com/geph-official/sosistab.git && cd sosistab && caogo build --release 
        
      - uses: actions/upload-artifact@v3
        with:
          name: upload
          path: sosistab/target/release/sosisbench
